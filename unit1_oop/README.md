# Unit 1 Discussion: Python OOP, Namespaces, and Copying

## Overview

This assignment explores object-oriented programming (OOP) concepts in Python, including inheritance, namespaces, and object copying.

## Learning Objectives

- Create parent and child classes
- Use inheritance to extend functionality
- Understand class and instance namespaces
- Demonstrate shallow and deep copying
- Apply object-oriented design principles

## Requirements

Complete all TODO sections in the source code:

1. Create a parent class.
2. Create a child class using inheritance.
3. Demonstrate class and instance namespaces.
4. Demonstrate shallow and deep copying.
5. Create and test objects in `main()`.
6. Add a student-created extension.

## Discussion Board Reflection

After completing the programming assignment, add this reflection to your initial discussion post in LEO.

Your reflection should be approximately 150–200 words and address the following questions:

1. What concepts or skills did you learn while completing this assignment?
2. What challenges did you encounter, and how did you overcome them?
3. Compare OOP to procedural programming.
4. Discuss the benefits of maintainability and reusability and apply this managing overhead, practical application development, and future use.

So I have experience with many languages, and my first jump into OOP was in Java, and since then I've been a huge fan of the approach. 
I tend to focus on thinking of OOP as creating objects that have certain properties, and using that approach of "This is an item and
this is it's properties" definitely makes programming in general much easier. Creating parent and child classes was also not new to me, given
that Java is something that heavily encourages the use of inherentance classes or subclasses, but also HTML sort of works in the same way. 
So for me, this wasn't intirely foreign of a concept, I just didn't know how to do it in Python. Learning how to do it in this language though 
makes Python just that much more easy to work with for me. I would say that was the main thing learned, how to take what I already knew as a concept
and apply that to Python.

The main challenege I encountered in this project was just the lists. Its been awhile and I had issues remembering how to format lists when they're
being printed, and that took a while to remember, but I also had issues with formatting in general. Eventually I knocked the rust off the gears and
figured out how to get the programs working. 

Procedural Programming is basically the opposite of OOP, in the sense that code functions in a sequence of algorithms rather than data objects where data is more separated
rather than tied together inside objects. That's not to say that one is better than the other, just that it's for different uses. OOP is much better for larger, full scale
applications, while PP is better for smaller scripts. With that in mind, I have no experience with any Procedural based languages. 

The reason why OOP is seen as more capable and reusable is because of how it works. For example, if you have a car, in OOP, that object has it's own internal state, and you can
do something like car.accelerate(speed) to modify it's speed property to whatever you need it to be. I think it's much more natural with how the human mind works, because what
you can essentially boil everything down to is taking a given object and tweaking it's properties, like with the car and it's speed. However, with Procedural Programming, a car
is just another data structure, where you would need to create a function dedicated to accelerations, accepting the car and it's speed as parameters, and so on. Again, different
functions for different uses, and this is why languages like Python can function in both ways if needed, but I've always found Object Oriented Programming to be much more logical
in it's function to a human, thus making it easier to code. 
